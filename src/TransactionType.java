/**
 * Represents the two possible types of transaction: credit (deposit) and debit (withdrawal).
 */
public enum TransactionType {
    WITHDRAW, // Represents a withdrawal transaction
    DEPOSIT   // Represents a deposit transaction
}